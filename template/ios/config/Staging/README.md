Place the staging `GoogleService-Info.plist` here after running:

```bash
flutterfire configure --project=<staging-project-id>
```

Expected filename:

```text
ios/config/Staging/GoogleService-Info.plist
```
