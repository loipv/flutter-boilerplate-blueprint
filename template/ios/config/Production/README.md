Place the production `GoogleService-Info.plist` here after running:

```bash
flutterfire configure --project=<production-project-id>
```

Expected filename:

```text
ios/config/Production/GoogleService-Info.plist
```
