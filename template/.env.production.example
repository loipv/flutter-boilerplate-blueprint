# Production environment variables.
# Copy this file to .env.production and fill in real values.
# .env.production is git-ignored and must never be committed.
#
# App Check debug tokens are used only when running the production flavor in
# debug mode on local devices. Register these UUIDs in Firebase Console under
# your production project's App Check settings, then keep the real attestation
# providers for release builds.
APP_CHECK_DEBUG_TOKEN_IOS=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
APP_CHECK_DEBUG_TOKEN_ANDROID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Analytics & Error Tracking
POSTHOG_API_KEY=phc_xxxx
# Optional. Leave blank for PostHog Cloud default, or set your region/self-hosted host.
# Examples:
# POSTHOG_HOST=https://us.i.posthog.com
# POSTHOG_HOST=https://eu.i.posthog.com
# POSTHOG_HOST=https://analytics.yourdomain.com
POSTHOG_HOST=
SENTRY_DSN=https://xxxx@xxxx.ingest.sentry.io/xxxx

# Google Sign-In (Android only, web OAuth client ID)
# Found in android/app/src/production/google-services.json
# -> oauth_client[] where client_type = 3
GOOGLE_SERVER_CLIENT_ID=xxxx.apps.googleusercontent.com

# Optional advanced auth setup. Set this only if you use a custom Firebase Auth
# redirect domain instead of the default generated one.
# Example:
# FIREBASE_AUTH_DOMAIN=auth.yourdomain.com
FIREBASE_AUTH_DOMAIN=

# Android Release Signing (see docs/android_signing.md)
UPLOAD_KEYSTORE_PATH=/Users/yourname/upload-keystore.jks
UPLOAD_KEY_ALIAS=upload
UPLOAD_STORE_PASSWORD=
UPLOAD_KEY_PASSWORD=
